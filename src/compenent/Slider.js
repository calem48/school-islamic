import React from 'react';

const Slider = () => {
    return (
        <div>
            i don't know we do it in this section we will talk about it later in the meet
        </div>
    );
}

export default Slider;
